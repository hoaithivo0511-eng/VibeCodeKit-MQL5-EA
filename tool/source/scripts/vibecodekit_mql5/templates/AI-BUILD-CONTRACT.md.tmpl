# AI-BUILD-CONTRACT — {project_name}

> Generated by `ai_build_contract`. This is the guard-rail the AI coding tool
> must obey. The canonical machine-readable form is `AI-BUILD-CONTRACT.json`.

## Source of truth

- EA-SPEC: `EA-SPEC.yaml` (status: {project_status})
- Risk bounds: see `risk` block in EA-SPEC (max_drawdown_pct required)

## Allowed paths (editable)

{allowed_paths}

## Forbidden paths (never hand-edit)

{forbidden_paths}

`evidence/` and `release/` are always forbidden.

## Forbidden claims

{forbidden_claims}

Never write these unless the project is genuinely RELEASE-ELIGIBLE with a valid
evidence hash chain.

## Forbidden behaviours

- Add a feature not in EA-SPEC.
- Silently change a risk rule.
- Remove a stop loss.
- Add martingale / grid / DCA when the spec forbids it.
- Claim PASS / READY / LIVE before evidence exists.

## Release evidence required

{release_evidence_required}

---

_Note: `ai_build_contract.render_contract_md` renders this contract from the
project EA-SPEC. This `.tmpl` documents the canonical field layout; the module
is the authoritative renderer (anti-bloat: no duplicate logic)._
