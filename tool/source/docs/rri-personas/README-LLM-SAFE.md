# LLM Safety Note

These files are workflow inputs/templates for the RRI and blueprint pipeline.

They are not test results, not validation evidence, and not proof that an EA has passed compile, backtest, or release gates.

Any PASS / READY / RELEASE claim is valid only when supported by `evidence/manifest.json` with `release_eligible=true` and artifact hashes.
