# TIP-001 — Implement audited RC7 remediation

## Builder scope

Implement RH-001 through RH-008 without changing EA trading behavior. Add
independent regression tests before synchronizing the packaged test snapshot.

## Acceptance commands

```bash
python -m pytest -q
python -m vibecodekit_mql5.selftest
ruff check --no-cache scripts tests
python scripts/maintenance/sync_distribution_snapshot.py verify
python scripts/maintenance/repo_manifest.py --check
```

Package verification must repeat the complete suite from an extracted source
archive and an installed wheel outside the checkout.
